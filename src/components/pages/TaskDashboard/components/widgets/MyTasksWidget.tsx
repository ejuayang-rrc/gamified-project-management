import DashboardWidgetCard from "../common/DashboardWidgetCard"

const MyTasksWidget = () => {

    return (
        <DashboardWidgetCard widgetTitle="MyTasks">
            <p>Populate this area with all the tasks assigned to the current user.</p>
        </DashboardWidgetCard>
    )
};

export default MyTasksWidget;